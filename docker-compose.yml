version: "3.9"
services:
  pipeline:
    build: .
    env_file:
      - .env          # loads your API key automatically
    volumes:
      - .:/app        # live code reload — edit locally, runs in container